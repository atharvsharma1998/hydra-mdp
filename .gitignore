# python
*.pyc
**/__pycache__/
.pytest_cache/*
.pydevproject

# IDE
.vscode/*

# Pip
*.egg-info

# files
*.log
*.pkl
*.jpg
*.pcd

# exp
exp

# data
dataset

# lsf
*.bsub

# running scripts
scripts_dev/*

# --- GTRS-BEVFusion additions: never commit data / weights / caches ---
navsim_workspace/
**/navsim_workspace/
trainval_sensor_blobs/
mini_sensor_blobs/
*_navsim_logs/
maps/
teacher_scores_cache/
metric_cache/
checkpoints/
runs/
logs/
eval_results/
# model weights / build / run artifacts
*.pth
*.ckpt
*.onnx
*.engine
*.out
wandb/
# keep the trajectory vocabulary (small, required at runtime):
!traj_final/
!traj_final/*.npy

# --- C++ deploy / CMake build artifacts (do not commit generated build output) ---
build/
deploy/build/
CMakeFiles/
CMakeCache.txt
cmake_install.cmake
CTestTestfile.cmake
Makefile
*.o
*.obj
*.so
*.so.*
*.a
*.plan
# large binary parity dumps (keep the small GT .txt references checked in)
deploy/parity-data/*.tensor
deploy/parity-data/*.bin

# JMLR MLOSS build artifacts
jmlr/submission/
jmlr/*.aux
jmlr/*.log
jmlr/*.bbl
jmlr/*.blg
jmlr/*.out
